# macOS
.DS_Store

# Claude Code (local-only settings)
.claude/settings.local.json

# Editor / IDE (root-level)
.idea/
*.iml
.vscode/

# Logs / env
*.log
.env
.env.local
.aider*

# 출시 산출물·자격증명 — 절대 커밋 금지 (AAB 바이너리, FCM 서비스 계정 키)
/release/
