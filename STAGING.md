# 스테이징 — 지금은 만들지 않는다

## 왜 안 만드나 (2026-08-25 판단)

스테이징의 가장 큰 값은 **마이그레이션을 운영 전에 리허설**하는 것인데, **CI가 이미 그 일을 합니다.**
`PostgresRepositoryTest`가 푸시마다 진짜 Postgres 컨테이너에 `V1~V49`를 전부 돌리고,
`ddl-auto: validate`로 엔티티와 마이그레이션의 어긋남까지 잡습니다.

그 위에 스테이징 백엔드가 더 주는 건 "실제 데이터 + 실제 앱으로 만져보기"입니다.
1인 운영에 유저가 아직 없는 지금은 값보다 비용이 큽니다:

- Render 무료는 **계정 전체 인스턴스-시간**을 나눠 씁니다. 운영 백엔드를 UptimeRobot으로
  24시간 깨워두고 있어 여유가 거의 없습니다 — 하나 더 깨우면 **둘 다 정지될 수 있습니다.**
- Supabase 프로젝트를 하나 더 관리해야 합니다(운영 DB 공유는 절대 금지 — 리허설이 곧 사고가 됩니다).
- 스키마·시드를 계속 두 곳에 맞춰야 합니다.

## 언제 만들면 되나

- **테스터가 생겨** 실제 데이터로 파괴적인 변경(데이터 이관, 만료 정책 변경 등)을 시험해야 할 때
- 운영 백엔드를 유료(Render Starter)로 올려 인스턴스-시간 걱정이 없어질 때
- 손대는 사람이 둘 이상이 될 때

## 만들 때 할 일

1. **Supabase 프로젝트를 새로** 만든다. ⚠️ 운영 DB 재사용 금지.
2. `render.yaml`에 `prologue-backend-staging`을 더한다(운영 서비스 블록을 복사, `plan: free`,
   `autoDeploy: true`). Render에서 Blueprint 재동기화 후 그 서비스에 새 DB 접속정보를 넣는다.
   **UptimeRobot으로 깨우지 말 것** — 필요할 때 첫 요청으로 깨우면 된다.
3. `app/eas.json`의 `preview` 프로파일에 스테이징 주소를 준다:
   `"env": { "EXPO_PUBLIC_API_URL": "https://<스테이징>.onrender.com" }`
4. `eas build -p android --profile preview` 한 번 — 운영 앱과 나란히 깔린다.
5. 이후로는 `eas update --branch preview`로 재빌드 없이 확인하고,
   `eas update --branch production`으로 승격한다.

## 다시 빌드해야 하는 때 (스테이징과 무관하게 늘 해당)

OTA는 JS만 실어 나릅니다. 아래는 **새 빌드**가 필요합니다.

- 네이티브 모듈을 새로 넣었을 때 — `expo-haptics`(2026-08-25 추가). 지금 스토어 빌드엔 없어
  **크래시는 없고 진동만 조용히 안 울립니다.**
- 네이티브 리소스 — 안드로이드 어댑티브 아이콘 48% 조정(2026-08-25).
- `app.json`의 플러그인·권한·버전 변경.

`runtimeVersion`이 `appVersion` 정책이라 **앱 버전을 올리면 이전 빌드는 새 OTA를 받지 못합니다.**
버전을 올릴 때는 스토어 배포와 짝을 맞추세요.
