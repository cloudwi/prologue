프롤로그 코드·UX/UI 검토 — 2026-09-07

현재 구조를 유지하면서 **실패했을 때의 동작, 로그인 상태, 공통 화면 규칙**을 먼저 맞추는 편이 효과적입니다. 색상·타이포 토큰, Query 캐시, Kotlin의 도메인/서비스/저장소 분리, Postgres 통합 테스트는 이미 있습니다. 새 체계를 만드는 것보다 기존 체계의 적용 범위를 넓히는 것을 권합니다.

검토 기준은 로컬 `44a385a`에서 시작한 `codex/review-consistency-ux` 브랜치입니다. 앱의 인증·세션·네 탭·공통 컴포넌트, 백엔드 모임 조회/신청/확정 경로, 웹 랜딩·운영 콘솔 구조, 디자인 문서와 CI를 살폈습니다. GitHub 플러그인으로 원격 저장소나 PR을 조회한 결과는 아닙니다. 전체 코드에 결함이 없음을 보증하는 전수 감사도 아닙니다.

**이번에 반영한 변경**

| 문제 | 변경 | 근거·검증 |
|---|---|---|
| 재발급 중 통신 실패·서버 장애도 토큰 삭제로 이어짐 | `/auth/refresh`의 인증 거절과 일시적 실패를 구분. 비정상 토큰 응답으로 저장소를 덮어쓰지 않음 | [API](app/src/lib/api.ts), [회귀 테스트](app/src/lib/api.test.ts): 연결 실패·429/500/503·401/403·잘못된 JSON 값·동시 재발급 |
| 프로필 조회 실패를 `null`로 삼켜 가입/소개팅 시작 안내가 나올 수 있음 | 첫 조회 실패를 오류로 전달하고 재시도 제공. 백그라운드 실패에서는 기존 프로필 유지 | [세션](app/src/lib/session.ts), [세션 테스트](app/src/lib/session.test.tsx) |
| 세션 만료·재로그인·탈퇴에서 캐시 정리가 일관되지 않음 | 만료 후 로그인 이동, 인증 성공, 탈퇴 성공 시 이전 계정의 Query 캐시 정리 | [공통 가드](app/src/lib/query.ts), [가드 테스트](app/src/lib/query.test.tsx), 로그인·탈퇴 화면 |
| 첫 조회 실패가 빈 목록/빈 내용처럼 표시됨 | 발견·모임·편지함·MY에 공통 실패 화면과 다시 시도 버튼 적용 | [ScreenLoadError](app/src/components/screen-load-error.tsx). 로컬 통신 실패와 정상 빈 목록을 별도 화면으로 확인 |
| 모임 회원이 재로그인하면 소개팅 안내가 있는 발견 탭으로 이동 | 선호 성별이 없는 회원은 모임 탭으로 이동. 명시적인 복귀 경로는 유지 | [자동 로그인](app/src/app/index.tsx), [이메일 로그인](app/src/app/email-auth.tsx) |
| 비회원 모임 탐색에서도 회원·직장 인증 API 요청 | 세션 확인 후 회원에게만 개인 정보 조회 | [모임 탭](app/src/app/(tabs)/meetups.tsx) |
| 웹 미리보기에서 지원하지 않는 캡처 차단 훅이 실행되어 오류 | 캡처 보호 컴포넌트를 네이티브에서만 실행. iOS/Android의 기존 보호 호출 유지 | [루트 레이아웃](app/src/app/_layout.tsx). 브라우저에서 오류 재현 후 해소 확인 |
| 공통 화면에도 글자 크기·굵기·라운드가 제각각 | MY 저장 동작과 제목 굵기, 가입 안내의 타이포/라운드를 기존 토큰에 맞춤. 시작 버튼에 접근성 역할 추가 | [SubScreen](app/src/components/sub-screen.tsx), [SignupGate](app/src/components/signup-gate.tsx), [디자인 규칙](design/README.md) |
| 모임 목록에서 모임마다 신청자 전체를 조회한 뒤 확정자만 필터링 | 표시할 모임들의 확정 참가자를 한 번에 조회하고 모임별로 묶음. 신청순 유지 | [MeetupService](backend/src/main/kotlin/com/prologue/backend/dailymeet/application/service/MeetupService.kt), [Postgres 테스트](backend/src/test/kotlin/com/prologue/backend/dailymeet/infrastructure/persistence/MeetupApplicationsBatchIT.kt). 참가자 조회가 모임 N개당 N회 → 1회 |
| 린트 경고 8개와 문서의 실제 구현 불일치 | 타이머 의존성·중복 import·미사용 타입·테스트 import 정리. CI에 경고 0개 기준 적용. README와 탭바 여백 설명 갱신 | [.github/workflows/ci.yml](.github/workflows/ci.yml), [README](README.md) |

**다음 개선의 우선순위**

1. **높음 — 정원 확정과 대기 인원 제한의 동시성.** `MeetupService.confirmApplication`은 확정 인원을 센 다음 별도 신청 행을 저장합니다. 마지막 한 자리에 대한 두 요청이 같은 인원수를 읽으면 둘 다 통과할 수 있습니다. `apply`의 대기 줄 제한도 같은 형태입니다. 현재 저장소에는 이를 직렬화하는 모임 행 잠금이나 버전 검사가 보이지 않습니다. 코드상 위험을 확인했으며, 동시 요청으로 실제 초과 확정되는 통합 재현은 아직 수행하지 않았습니다. 모임 단위 잠금/원자적 갱신을 설계하고, 확정·신청 취소·정원 변경을 함께 검증해야 합니다. 완료 기준: 마지막 한 자리 동시 확정 시 한 요청만 성공, 성별 정원과 취소 경합도 보장.

2. **중간 — 작은 CTA 글자의 색 대비.** 라이트 테마의 흰 글자와 `#D9694C` 조합은 계산상 약 **3.46:1**입니다. 발견의 답변 저장, 시작/가입 버튼처럼 작은 글자에는 부족합니다. `#C25539` 위 흰 글자는 약 4.51:1이지만 눌림을 opacity로 처리하면 다시 달라집니다. 일반 글자의 기준은 4.5:1입니다. [W3C 대비 기준](https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum). 장식용 primary와 버튼 면/글자/눌림 상태를 별도 토큰으로 정하고 공통 버튼에 적용하는 것을 권합니다. 실제 기기·접근성 크기에서의 검증은 별도로 필요합니다.

3. **중간 — 웹의 이용 설명과 앱의 실제 기능.** [랜딩](web/src/pages/index.astro)의 “편지에만 잉크가 들어요”, “답을 남겨야 상대의 답도 열립니다”는 [잉크 정책](app/src/lib/ink.ts)의 프로필 재열람(8), 답변 열람(5)과 맞지 않습니다. “문답과 호감은 무료, 편지와 추가 열람에는 잉크를 사용해요”처럼 실제 선택지를 설명해야 합니다. FAQ 본문과 구조화 데이터, 첫 화면의 짧은 문구를 함께 맞추는 것이 완료 기준입니다.

4. **중간 — 남은 목록 조회 비용.** 확정 참가자 조회는 이번에 묶었지만, 같은 서비스의 모임별 회차·모임장 완료 횟수·참가자 프로필/직장 인증 조회는 남아 있습니다. 모임/회차/계정 ID를 모아서 조회한 뒤 map으로 조립하는 방향을 권합니다. 운영 응답 속도를 측정한 결과는 아니므로, 먼저 10/50/100개 모임의 SQL 수와 응답 시간을 비교해야 합니다. DTO 내용·참가자 순서·공개 범위를 유지하면서 조회 수를 줄이는 것이 기준입니다.

5. **중간 — 보조 조회의 실패를 정상 값으로 바꾸는 처리.** 모임의 지난 기록 조회 실패는 `[]`, 직장 인증 실패는 `verified: false`로 바뀝니다. 사용자가 “이력이 없다”, “인증되지 않았다”로 받아들일 수 있습니다. `unknown/error` 상태를 정상적인 빈 값과 분리하고, 주요 목록은 유지하면서 해당 영역만 재시도할 수 있게 하는 것을 권합니다. 첫 조회 실패 처리는 이번에 공통화했지만, 부분 실패/백그라운드 갱신 안내까지 모두 바꾼 것은 아닙니다.

6. **중간 — 상태/정책의 중복 정의.** 나이 계산은 모임 탭과 `profile-form.ts`, 가격은 앱/서버/웹 설명, 날짜·종료 시각 문구는 앱/서버 HTML/콘솔에 나뉩니다. 재사용 가능한 순수 함수를 먼저 모으고, 언어를 넘는 정책은 API 응답이나 공통 계약 사례로 맞추는 편이 좋습니다. 단순히 하나의 거대한 공유 파일로 합치기보다는 날짜·가격·참가 조건의 경계를 유지해야 합니다.

7. **보통 — 화면과 서비스의 책임 분리.** `host.astro`는 2,859줄에 편집·미리보기·참가자 관리·통신이 함께 있고, 발견 화면은 약 1,000줄, `MeetupService`는 약 1,000줄입니다. 길이 자체보다 변경 이유가 섞인 것이 문제입니다. 콘솔은 API/편집 폼/참가자 관리, 발견은 문답 편집/상대 카드/데이터 훅, 서버는 조회/참가/운영 검수부터 나누는 것을 권합니다. 동작을 바꾸는 수정과 파일 이동을 별도 단위로 검증하면 리뷰가 쉬워집니다.

8. **보통 — 디자인 토큰 적용과 문서의 예외 정리.** 직접 지정한 fontSize와 borderRadius가 여러 화면에 남아 있습니다. 로고·초대장 날짜처럼 의도적인 예외를 제외하고, 공통 입력/버튼/카드부터 토큰을 적용하는 편이 좋습니다. 공개 랜딩의 세리프 제목과 앱·운영도구의 기본체는 이미 다른 의도가 있으므로 예외를 문서에 명확히 남기세요. 이번에는 공통 오류·가입 안내·MY 저장 영역부터 적용했습니다.

**검증 결과와 범위**

- 앱: TypeScript 통과, ESLint 경고/오류 0개, Jest **88개 통과**(기존 69개 + 인증/세션 회귀 19개).
- 백엔드: Gradle **431개 통과**, 실패·오류·스킵 0개. 실제 Postgres 통합 테스트 포함. 최초 기준선은 `--rerun-tasks`로 실행했고, 변경 후에도 테스트를 다시 실행했습니다.
- 웹: Vitest **33개 통과**, Astro **24개 페이지 빌드 성공**. 웹 소스는 이번 변경에서 수정하지 않았습니다.
- 화면: 웹 랜딩의 기본 데스크톱 폭과 모바일 390px 폭, Expo 웹의 비회원 모임 탐색·오류/빈 목록·가입 안내를 확인했습니다. 앱 API는 별도 로컬 주소와 테스트용 빈 목록으로 검증했습니다.
- iOS/Android 실기기, 실제 회원 로그인·결제·알림 발송, 운영 API 성능, 동시 정원 확정은 이번 실행의 검증 범위에 포함되지 않습니다.
- 앱 변경은 아래 production OTA로 발행했습니다. 백엔드 변경은 `main` 반영 후 Render 배포로 적용하며, CI·문서는 원격 저장소 반영 대상입니다.

**OTA 발행 — 2026-09-07 15:40 KST**

- iOS·Android, `production` 채널/브랜치, `production` 환경, 런타임 `1.3.0`으로 발행 완료. 원격 조회에서 활성 운영 채널의 최신 업데이트가 두 플랫폼 모두 아래 그룹인 것을 확인했습니다.
- [Expo 발행 내역](https://expo.dev/accounts/cloudwi/projects/prologue/updates/85e51594-491b-4524-8b6c-3083cd0e4542): `85e51594-491b-4524-8b6c-3083cd0e4542`.
- 발행 메시지: “로그인 안정성·세션 캐시·공통 오류 및 안내 화면 개선”. 기준 커밋 `44a385afbdd19e62840ff7e86180c68e0bc5e4a9`와 검증한 로컬 앱 변경을 함께 발행했습니다. 새 Git 커밋을 만든 배포는 아닙니다.
- 새 스토어 빌드는 필요하지 않습니다. 이전 production OTA와 런타임 및 네이티브 fingerprint가 iOS·Android 모두 같습니다. 앱 의존성·네이티브 설정 변경도 없습니다. 적용 대상은 production 채널의 런타임 `1.3.0` 앱입니다.
- 배포용 두 플랫폼 번들에서 운영 API `https://api.prologue.day` 사용 및 테스트 API 주소 미포함을 확인했습니다. 번들 생성·업로드·발행 성공. 실기기에서 다운로드와 실행 완료 여부는 확인하지 않았습니다.
- 이전 OTA 그룹: `522d693e-d446-4ba3-a27b-99065ccc3e3a` (“프로필 완성도 + 스플래시 이음매”, 2026-09-02). 필요 시 복구 기준으로 사용할 수 있습니다.
- 백엔드 참가자 일괄 조회 개선은 OTA에 포함되지 않는 별도 서버 배포 대상입니다. 데이터베이스 구조나 API 응답 형식 변경은 없습니다.
