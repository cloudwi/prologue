-- 자기소개를 300자에서 500자로.
--
-- 한 문단으로 자기를 소개하기에 300자는 좁다. 취미와 사는 모습, 어떤 사람을 만나고 싶은지를
-- 담다 보면 금방 닿는다. 늘리는 쪽은 기존 글에 아무 영향이 없다 — 짧은 글은 그대로 짧다.
--
-- 오늘의 답변은 컬럼이 이미 1000이라 손댈 것이 없다(도메인 상한만 300 → 500).
alter table members
    alter column bio type varchar(500);
