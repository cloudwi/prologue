-- 소개 글 사이에 놓는 사진.
--
-- 커버는 표지다 — 모임 탭의 카드 얼굴이자 초대장 맨 위 한 장. 그건 세 장이면 넉넉하다.
-- 여기 담기는 건 다른 것이다. "이 서점에서 만나요", "지난 회차는 이랬어요" 처럼
-- 글의 흐름 속에 놓여야 뜻이 사는 사진들. 표지와 섞어두면 무엇이 카드에 걸릴지 알 수 없다.
--
-- 소개 글에 [사진1] 같은 표시를 넣으면 그 자리에 이 목록의 사진이 들어간다. 글은 여전히
-- 평문이라 저장 형식이 바뀌지 않고, 사진은 커버와 같은 업로드 경로를 써서 검수도 그대로 걸린다.
alter table meetups add column body_image_urls text;

comment on column meetups.body_image_urls is '소개 글 안에 넣는 사진 URL 목록(쉼표 구분). [사진N] 표시가 가리키는 대상';
